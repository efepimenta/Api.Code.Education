{% include 'header.twig' %}
{% if not logado %}
    <h4>Favor logar para fazer alterações</h4>
{% else %}
    <h1>Lista de Tags</h1>
    <table class="table table-bordered table-striped">
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Controle</th>
        </tr>

        {% for tag in tags %}
            <tr>
                <td>{{ tag.id }}</td>
                <td>{{ tag.tag }}</td>
                <td>
                    <button {% if not logado %}disabled="disabled"{% endif %} class="btn btn-danger btn-xs"
                            data-toggle="modal" data-target="#delete{{ tag.id }}">Excluir
                    </button>
                </td>
                <form action="{{ path() }}tag/excluir" method="post">
                    <input type="hidden" name="id" value="{{ tag.id }}">
                    <!-- Modal -->
                    <div class="modal fade" id="delete{{ tag.id }}" tabindex="-1" role="dialog"
                         aria-labelledby="myModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal"><span
                                                aria-hidden="true">&times;</span><span class="sr-only">Close</span>
                                    </button>
                                    <h4 class="modal-title" id="myModalLabel">Editando
                                        <strong>{{ tag.tag }}</strong></h4>
                                </div>
                                <div class="modal-body">

                                    Deseja reamente remover <strong>{{ tag.tag }}</strong>?

                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                                    <button type="submit" class="btn btn-danger">Remover</button>
                                    <input type="hidden" name="acao" value="excluir">
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </tr>
        {% endfor %}
    </table>
    <br>
{% endif %}

{% include 'footer.twig' %}